        /* 
            stage('Deploy to Tomcat') {
            steps {
                echo 'Deploying WAR file to Tomcat...'
                script {
                    // Find the WAR file generated by the Maven build.
                    // This assumes the WAR file is in the 'target' directory and ends with '.war'.
                    // Adjust the pattern if your WAR file name is different (e.g., specific version).
                     // def warFile = findFiles(glob: 'target/**/*.war')

                /*    if (warFile.length == 0) {
                        error 'No WAR file found for deployment. Please check your build process.'
                    } else if (warFile.length > 1) {
                        echo "Warning: Multiple WAR files found. Deploying the first one: ${warFile[0].path}"
                    }

                    // Use the 'deploy' step provided by the "Deploy to container" plugin.
                    // This step requires:
                    // - adapters: Specifies the container type (Tomcat8, Tomcat9, etc.) and its URL.
                    // - contextPath: The path where the application will be deployed on Tomcat.
                    // - credentialsId: The Jenkins Credentials ID for authenticating with Tomcat Manager.
                    // - war: The path to the WAR file to be deployed.
                    deploy adapters: [
                        tomcat11(
                            url: 'http://localhost:8080/manager/text', 
                            credentialsId: '827e4688-d704-46d8-9602-490aeaa45d23'
                        )
                    ],
                    contextPath: '/my-application',
                    war: warFile[0].path // Deploy the first found WAR file

                }
                echo 'Deployment to Tomcat completed.'
            }
        }
        */

        // ****************
